node_modules/
*.log

# Local index artifacts (uploaded to GitHub releases, not committed)
index/lbug
index/meta*.json
gitnexus-index-*.tar.gz

# Local environment / compose overrides — user-specific paths, not shared.
# Templates are committed as .env.example and docker-compose.override.yml.example.
.env
docker-compose.override.yml
